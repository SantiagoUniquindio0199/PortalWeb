import { DisablControlDirectiveDirective } from './disable-control-directive.directive';

describe('DisablControlDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisablControlDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
